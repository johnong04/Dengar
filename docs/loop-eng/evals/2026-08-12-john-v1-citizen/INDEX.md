# Eval cases — v1-citizen run (2026-08-12)

One line per finding: real | noise — source — what.

- real — slice3-eval F1 (MAJOR) — abstain readouts were a hardcoded display-map; plan hard case demanded pipeline values. Structural: Verdict abstain arm carried no numbers. Fix: readings{} on abstain arm, red-first. Grep to find: `ABSTAIN_COPY` seeded constants in result.tsx.
- noise — slice3-eval F3 (INFO) — reduced-motion first-frame relies on reanimated auto-disable; measured correct. Do not demand a custom guard.
- noise — slice2-eval F1 (minor) — reanimated kill-switch makes bespoke reduced-motion opacity loops dead code; the static fallback is the sanctioned behavior, not a defect.
- real — slice3 fix1 verified — readings fix landed red-first; re-verdict PASS. Runnable case: gating.check.ts abstain readings asserts (the red test itself).
- noise — slice4-eval F3 (INFO) — "fourteen detections/72h" flagged as modeled-as-fact; it is plan copy from specs §8's declared-simulation scenario. Narration owns the honesty, not the screen. Do not demand a citation chip in-app.
- real — slice8 self-critique — result.tsx drench used off-scale spacing (mt-14/mt-10), same defect class slice-2 eval flagged in capture; fixed. Pattern: off-scale spacing recurs → candidate for a lint/grep gate instead of reviewer attention (retro).

## v2-full run
- real — slice13-eval F1 (HIGH) — heat grid column sums (2,2,3,6,4,7,10) contradict the trend series (2,1,2,3,4,4,6) and the KPI in the same frame: today's column says 10 detections, chart and KPI say 6. Runnable case: assert sum(heat column c) === DETECTIONS_14D[7+c] for all c. Fix: derive the grid, never hand-type it.
- real — slice13-eval F2 (HIGH) — watch-area figures don't reconcile: TM delta +6 vs derived +8; Wangsa Maju count 3 vs its own sparkline's 2; area counts total 17 against a district 72h total of 14; TM's spark is byte-identical to DETECTIONS_14D, asserting TM = whole district. Runnable case: assert DETECTIONS_14D is the element-wise sum of the area sparks, and every count/delta derives from them.
- real — slice13-eval F3 (MEDIUM) — Danau Kota "0 · 11 h" vs a sparkline whose last non-zero is 6 days back. Ambiguous column semantics (node freshness vs detection recency) rendered as fact.
- real — slice13-eval F6 (LOW, judged) — "TREND" eyebrow is the banned reflex (labels a chart already labelled by its legend and axis); "Hour × day" and "Watch areas" are legitimate wayfinding. The evaluator argued the distinction instead of applying the ban mechanically — that is the behaviour to preserve when re-tuning this reviewer.
- noise — slice13-eval prose-count discrepancy — maker claimed 29 words, evaluator reproduced 21/32/77 under three counting rules. The floor (≤40) holds under all of them; do not treat a word-count mismatch as a finding.
- real — slice12-eval F1 (MAJOR) — the capture pulse NEVER rendered: react-native-web drops `className` on a reanimated Animated.View, so both rims had width 0/height 0/borderTopWidth 0 while animating correctly. Survived slice 2's evaluator, slice 8's polish AND slice 12's own maker. Runnable case: assert computed width/height/borderTopWidth of the animated rims are non-zero, and that two screenshots ~700ms apart differ. Fix: geometry+border on `style`, not className (the codebase already documents this at result.tsx:298).
- real — slice12-eval F6 (TRIVIAL but in-class) — one contrast pair carries three different numbers across design-system.md (4.77), tailwind.tokens.js (4.93) and the built layout (7.17). Numbers are a declared correctness surface here; a stale measured figure in law is the same defect class as an invented one.
- noise — slice12-eval "FillTrack clamps at 1.0" — flagged as worth-knowing, not a finding. A track that saturates above the floor is honest; do not demand a proportional readout above threshold.

## v2-full run — session 2 (slices 15, 16, 18, 19, 20 + orchestrator)
- real — orchestrator vs slice20 comment (MEDIUM, refuted-by-measurement) — slice 20 shipped a source comment asserting react-native-web drops escaped-dot Tailwind classes and renders `h-3.5` at height 0. False: `h-1.5` measures 6px live, `mt-1.5` works on the same line, and the bars render 350x14 with the fill at 135.1px = 38.6% (the stated share). The SCREEN was correct; only the stated reason was invented. Class: a false hard-won fact in a shipped comment propagates to every future agent as a bug that does not exist — this one was one step from being written into RESUME.md as law. Runnable case: assert computed height of a `h-1.5` element === 6. Do not confuse this with the real Animated.View className defect (slice12-eval F1) — that one is genuine and differently scoped.
- real — slice18 F1 (MEDIUM) — specs §2 language sweep passes in English and FAILS in Malay: `tinjauan` in the forecast kicker reads as "survey" where the English "outlook" does not breach. Class: the §2 sweep must run per-language, not once on the source language. This is the first finding that only exists because a second language exists. Runnable case: grep ms.ts for the §2 banned-sense list, not just en.ts.
- real — slice18 F2 (MEDIUM) — abstain "Audio kept" row wrapped both halves in BM: ~361px of text in a 310px row. Malay-longer-than-English is the predicted failure and it landed exactly once across 19 route/states. Runnable case: assert the row renders on one line at 390 with language=ms.
- real — slice18 F3 (LOW) — `/history` collapsed row printed the raw `female` token rather than copy. Found only because every string moved behind a lookup; the migration is itself the detector.
- real — slice16 self-catch (MEDIUM) — `fitFocus` scales by width alone, so a fixed-height map box frames correctly at 390 and slices the neighbourhood outline off at 430. Panel height now derives from the projected outline. Runnable case: assert the projected outline's bbox fits the panel at both 390 and 430. Any future screen putting `fitFocus` in a fixed-height box inherits this.
- real — cross-slice figure consistency (MEDIUM) — slices 19 and 20 both print the citizen-science vs ovitrap cost gap. Slice 20 matched slice 19's exact 7.6x rather than §9's rounded "8x", and both name the rounding. Class: one app must not carry two numbers for one fact; the second slice to touch a figure must read what the first shipped.
- noise — slice18 "map-container crop" at 430 on `/area` and `/officer/cluster/[id]` — the OSM raster's intended `overflow-hidden` crop. Fires identically in EN, and one instance contains no text at all. A clipped-text probe will keep reporting it. Do NOT treat a deliberate raster crop as a copy-overflow defect.
- real — orchestrator process (LOW, non-code) — with no `.prettierrc`, bare `npx prettier --write` reformatted to double-quote/80-col against a single-quote/100-col tree. TWO concurrent agents each discovered it independently, each hand-passed flags to work around it, and NEITHER reported it, because neither one's own slice went red. Class: a hazard every agent routes around privately is invisible to the run — the orchestrator only sees it when two reports happen to mention the same workaround. Fixed by committing `app/.prettierrc`.
