import { Text, View } from 'react-native';

// PLACEHOLDER — a later slice builds the real 3-step onboarding + mic permission here.
export default function Onboarding() {
  return (
    <View className="flex-1 items-center justify-center bg-bg px-5">
      <Text className="font-plex-semibold text-xl text-ink">Onboarding</Text>
    </View>
  );
}
