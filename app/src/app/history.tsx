import { Text, View } from 'react-native';

// PLACEHOLDER — a later slice renders the real history list here.
export default function History() {
  return (
    <View className="flex-1 items-center justify-center bg-bg px-5">
      <Text className="font-plex-semibold text-xl text-ink">History</Text>
    </View>
  );
}
